project:
  local:
    hostname: local.cardinalservice.com
    protocol: http
  machine_name: cardinalservice
  human_name: cardinalservice
drush:
  aliases:
    local: cardinalservice.local
    remote: cardinalservice.prod
drupal:
  db: {  }
