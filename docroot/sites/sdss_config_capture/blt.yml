project:
  machine_name: sdss_config_capture
  human_name: sdss_config_capture
  local:
    protocol: http
    hostname: local.sdss_config_capture.com
drush:
  aliases:
    local: sdss_config_capture.local
    remote: sdss_config_capture.prod
drupal:
  db: {  }
